# DeepVisionAttend - AI Service dependencies
# Python 3.11+ required.
#
# Install with:
#   pip install --upgrade pip setuptools wheel
#   pip install -r requirements.txt
#
# NOTE: insightface builds a small C extension during install, so it needs
# Cython, numpy and a C++ compiler present *before* pip reaches it. They are
# listed first for that reason. On macOS run `xcode-select --install` once.

# --- Build prerequisites for insightface -----------------------------------
cython>=3.0.0
numpy>=1.26.0,<3.0.0

# --- Deep learning face recognition (pre-trained; inference only) -----------
insightface>=0.7.3          # SCRFD detector + ArcFace embeddings
onnxruntime>=1.18.0         # CPU inference. For NVIDIA GPUs, replace this line
                            # with: onnxruntime-gpu>=1.18.0  (and set DVA_CTX_ID=0)

# --- Computer vision --------------------------------------------------------
opencv-python>=4.9.0        # Camera capture, image decoding, drawing
scikit-image>=0.22.0        # Required by insightface's alignment utilities

# --- REST API ---------------------------------------------------------------
fastapi>=0.110.0            # API framework with automatic OpenAPI docs
uvicorn[standard]>=0.27.0   # ASGI server
python-multipart>=0.0.9     # multipart/form-data (image uploads)

# --- Configuration and validation -------------------------------------------
pydantic>=2.6.0             # Request/response models and validation
pydantic-settings>=2.2.0    # Environment-driven configuration
python-dotenv>=1.0.0        # .env file support

# --- Development and testing (optional but recommended) ---------------------
pytest>=8.0.0
httpx>=0.27.0               # Required by fastapi.testclient

# --- Authentication ---------------------------------------------------------
pymongo>=4.6.0              # MongoDB driver (user accounts)
bcrypt>=4.1.0               # Password hashing
pyjwt>=2.8.0                # Access tokens
email-validator>=2.1.0      # EmailStr validation in the auth schemas
